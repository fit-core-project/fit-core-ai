[pytest]
markers =
    slow: long-running tests not required for the normal development loop
    integration: tests that touch multiple components, subprocesses, or local services
    llm: tests that require model loading or LLM inference
    chroma: tests that access ChromaDB or an embedding index
    smoke: manual/local smoke tests for deployed or running services
    eval: offline evaluation harness tests
